import { Container } from '@mui/material';
import GrowCardPrint from '@/components/GrowCardPrint';

export default function GrowCardsPage() {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <GrowCardPrint />
    </Container>
  );
}

