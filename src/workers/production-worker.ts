/**
 * Production Worker
 * Handles production task execution
 */

import { productionWorker } from '@/lib/job-queue';

// Production worker is already initialized in job-queue.ts
console.log('🌱 Production worker initialized');
