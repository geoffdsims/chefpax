/**
 * Notification Worker
 * Handles notification job execution
 */

import { notificationWorker } from '@/lib/job-queue';

// Notification worker is already initialized in job-queue.ts
console.log('📧 Notification worker initialized');
