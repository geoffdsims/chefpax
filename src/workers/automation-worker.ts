/**
 * Automation Worker
 * Handles automation job execution
 */

import { automationWorker } from '@/lib/job-queue';

// Automation worker is already initialized in job-queue.ts
console.log('🤖 Automation worker initialized');
