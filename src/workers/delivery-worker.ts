/**
 * Delivery Worker
 * Handles delivery job execution
 */

import { deliveryWorker } from '@/lib/job-queue';

// Delivery worker is already initialized in job-queue.ts
console.log('🚚 Delivery worker initialized');
